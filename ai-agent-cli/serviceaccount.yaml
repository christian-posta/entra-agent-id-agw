apiVersion: v1
kind: ServiceAccount
metadata:
  name: ai-agent-cli-sa
  namespace: entra-demo
  annotations:
    # The client ID of the Blueprint app that will be trusted for workload identity
    azure.workload.identity/client-id: "${BLUEPRINT_APP_ID}"
  labels:
    azure.workload.identity/use: "true"

