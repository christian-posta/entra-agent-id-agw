# Azure Entra Agent ID demo

This demo has a CLI AI agent that can use MCP tools to perform a task. 

The agent requires a valid user (SSO) to login via Microsoft Entra. The OAuth flow is done via a device login flow. The user can then prompt the agent and the agent has access to MCP tools. 